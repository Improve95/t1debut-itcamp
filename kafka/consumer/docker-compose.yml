services:
  weather-statistics-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weather-statistics-service
    env_file:
      - .env
    ports:
      - ${WEATHER_EVENT_PORT}:8080